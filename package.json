{
  "scripts": {
    "dev": "next dev",
    "build": "next build",        // ← збираємо лише build
    "start": "next start",
    "export": "next build"        // ← на випадок, якщо amplify.yml викличе export
  }
}
